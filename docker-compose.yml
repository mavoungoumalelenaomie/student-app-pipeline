version: '3'
services:
  app:
    build: .
    ports:
      - "5000:5000"
  prometheus:
    image: prom/prometheus
    volumes:
      - /opt/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9092:9090"
  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
